"use client";

/**
 * @deprecated Import from `@/components/apps` — AppShelf is now AppFolder.
 * Kept so older docs / imports keep resolving during the transition.
 */
export { AppFolder as AppShelf, AppFolder } from "@/components/apps";
